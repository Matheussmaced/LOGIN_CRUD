generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Person {
  userId   Int    @default(autoincrement())
  email    String @unique
  password String @unique
}
